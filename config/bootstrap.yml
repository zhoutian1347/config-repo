spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: item-service
      name: item-service
      password: dev
      
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka,http://eureka2:2002/eureka
